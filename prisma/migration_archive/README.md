# Prisma Migration Archive

This archive preserves migration files that are no longer part of the active Prisma migration path.

## 20240101000000_init_outdated

The archived `20240101000000_init` migration is outdated and does not represent the current production schema in Neon.

Do not replay this migration against production or use it as a baseline for the current database.

The replacement active baseline migration is:

```text
prisma/migrations/20260714120000_baseline_current_production
```
